source template
source variables

if [ "$serverType" == "Webserver" ]
	then template $(dirname $0)/webserver.template > /etc/hosts
fi

if [ "$serverType" == "Webserver" ]
	then template $(dirname $0)/webserver.template > /etc/hosts
fi
