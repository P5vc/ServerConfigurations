mo opendkim.conf > /etc/opendkim.conf
mo signing.table > /etc/opendkim/signing.table
mo key.table > /etc/opendkim/key.table
mo trusted.hosts > /etc/opendkim/trusted.hosts
mo opendkim > /etc/default/opendkim
